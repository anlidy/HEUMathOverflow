# HEUOverflow 本地完整部署测试指南

## 整体架构
```
前端 (Vue 3 + Vite)
  ↓
代理到后端服务:
  - user-service (8081)
  - forum-service (8082)
  - audit-service (8083)
  ↓
基础设施 (Docker):
  - PostgreSQL (5432)
  - Redis (6379)
  - MongoDB (27017)
  - MinIO (9000/9001)
  - Elasticsearch (9200)
  - Kibana (5601)
  - RabbitMQ (5672/15672)
```

## 前置要求

- **Docker** 和 **Docker Compose** (v2+)
- **Node.js** 20.19.0 或 22.12.0+
- **Go** 1.24.x（如需在本地编译）

## 一、后端部署

### 1. 准备环境变量

进入后端 docker 目录：
```bash
cd backend/docker
```

检查 `.env` 文件是否存在，如果不存在则复制：
```bash
# Linux/Mac
cp .env.example .env

# Windows
copy .env.example .env
```

`.env` 文件内容（已包含默认值）：
```dotenv
# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# postgres dashboard
PGADMIN_DEFAULT_EMAIL=nomail@example.com
PGADMIN_DEFAULT_PASSWORD=admin123

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Mongo
MONGO_USER=mongoadmin
MONGO_PASSWORD=mongoadmin

# RabbitMQ
RABBIT_USER=rabbitadmin
RABBIT_PASSWORD=rabbitadmin
```

### 2. 启动后端服务（推荐方式）

**Linux/Mac 用户**：

```bash
# 进入 backend 目录
cd backend

# 启动所有服务
./bin/run up --build -d

# 查看容器状态
docker ps

# 查看日志
docker compose -p backend logs -f user-service
```

**Windows 用户**：

```cmd
# 进入 backend 目录
cd backend

# 方式A：使用 Python 脚本（推荐，更稳定）
python bin\run.py up --build -d

# 方式B：使用批处理脚本
bin\run.cmd up --build -d

# 查看容器状态
docker ps

# 查看日志
docker compose -p backend logs -f user-service
```

### 3. 手动启动（如果脚本失败）

```bash
cd backend/docker

# 启动基础设施（PostgreSQL, Redis, MinIO, MongoDB 等）
docker compose -p backend -f docker-compose.yml up -d

# 启动应用服务（user-service, forum-service, audit-service 等）
docker compose -p backend -f docker-compose.service.yml up --build -d

# 查看所有容器
docker ps
```

### 4. 验证后端服务

等待所有容器启动完毕（约 30-60 秒），然后测试：

```bash
# 测试 user-service
curl http://localhost:8081

# 测试 forum-service
curl http://localhost:8082

# 测试 audit-service
curl http://localhost:8083
```

**期望输出**：如果服务正常运行，会收到 HTTP 404 或其他 API 响应（而不是连接拒绝）

### 5. 访问管理工具

| 工具 | 地址 | 账号 | 密码 |
|------|------|------|------|
| PgAdmin (PostgreSQL 管理) | http://localhost:18080 | nomail@example.com | admin123 |
| MinIO 控制台 | http://localhost:9001 | minioadmin | minioadmin |
| Kibana (Elasticsearch 管理) | http://localhost:5601 | - | - |
| RabbitMQ 管理界面 | http://localhost:15672 | rabbitadmin | rabbitadmin |

---

## 二、前端部署

### 1. 安装依赖

```bash
cd frontend

# 使用 npm
npm install

# 或使用 yarn
yarn install
```

### 2. 启动开发服务器

```bash
# 启动前端开发服务器
npm run dev

# 或
yarn dev
```

**输出示例**：
```
  VITE v7.1.11  ready in 234 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h + enter to show help
```

前端会在 `http://localhost:5173` 启动，并自动打开浏览器。

### 3. 前端代理配置

前端的 `vite.config.ts` 已配置 API 代理，自动将以下请求转发到后端：

```
/api/v1/user  →  http://localhost:8081
/api/v1/forum →  http://localhost:8082
/api/v1/audit →  http://localhost:8083
```

所以前端代码中可以直接使用相对路径调用 API，如 `/api/v1/user/login`。

---

## 三、完整测试流程

### 启动顺序：

**第一步**：启动后端（在一个终端）
```bash
cd backend
./bin/run up --build -d  # Linux/Mac
# 或
python bin\run.py up --build -d  # Windows
```

**第二步**：验证后端服务（等待约 30-60 秒）
```bash
docker ps  # 查看容器是否都 running
curl http://localhost:8081  # 测试服务连通性
```

**第三步**：启动前端（在另一个终端）
```bash
cd frontend
npm install  # 首次需要安装依赖
npm run dev
```

**第四步**：访问应用
- 打开浏览器访问 `http://localhost:5173`
- 前端会自动代理 API 请求到后端服务

---

## 四、常见操作

### 查看日志

```bash
# 查看特定服务日志
docker compose -p backend logs user-service

# 实时查看日志
docker compose -p backend logs -f user-service

# 查看所有服务日志
docker compose -p backend logs -f
```

### 重启服务

```bash
# 重启特定服务
docker compose -p backend restart user-service

# 重启所有服务
docker compose -p backend restart
```

### 停止服务

```bash
# 停止所有服务
cd backend
./bin/run down  # Linux/Mac
# 或
python bin\run.py down  # Windows

# 或手动停止
docker compose -p backend -f docker-compose.service.yml down
docker compose -p backend -f docker-compose.yml down
```

### 进入容器调试

```bash
# 进入某个容器
docker exec -it backend-user-service-1 sh

# 查看容器网络
docker network inspect backend_default
```

### 清理所有容器和镜像

```bash
# 清理未使用的镜像和容器
docker system prune -a

# 删除所有数据（包括数据库数据）
docker volume prune -a
```

---

## 五、故障排查

### 问题 1: 容器无法启动

```bash
# 查看错误日志
docker compose -p backend logs user-service

# 检查容器状态
docker ps -a

# 重新构建镜像
docker compose -p backend -f docker-compose.service.yml up --build -d
```

### 问题 2: 前端无法访问后端 API

1. 确认后端服务都在运行：
   ```bash
   docker ps  # 应该看到 user-service, forum-service, audit-service
   curl http://localhost:8081  # 测试连通性
   ```

2. 检查浏览器控制台的网络请求（F12 → Network 标签）

3. 确认 vite 代理配置正确（见 `frontend/vite.config.ts`）

### 问题 3: 数据库连接失败

- 检查 `.env` 文件中的数据库配置是否正确
- 确保 PostgreSQL 容器在运行：`docker ps | grep postgres`
- 进入 PostgreSQL 容器测试连接

### 问题 4: 端口被占用

```bash
# 查看占用端口的进程
lsof -i :8081  # Linux/Mac
netstat -ano | findstr :8081  # Windows

# 改为使用其他端口，修改 docker-compose.service.yml 中的 ports 配置
```

---

## 六、开发工作流

### 后端开发

如果需要热重载开发，请改用 `docker-compose.watch.yml`：

```bash
cd backend/docker
docker compose -p backend -f docker-compose.watch.yml up --build
```

这会启用文件监控，代码改动时自动重新编译和重启服务。

### 前端开发

前端开发服务器已配置热模块替换（HMR），代码改动会自动刷新：

```bash
cd frontend
npm run dev
```

### 生产构建

```bash
# 前端生产构建
cd frontend
npm run build

# 构建输出在 frontend/dist 目录
```

---

## 七、关键路径及配置文件

```
HEUOverflow/
├── backend/
│   ├── cmd/                    # 各服务启动入口
│   │   ├── user-service/
│   │   ├── forum-service/
│   │   ├── audit-service/
│   │   └── es-service/
│   ├── internal/               # 各服务的业务逻辑
│   ├── docker/                 # Docker 配置
│   │   ├── docker-compose.yml           # 基础设施（数据库等）
│   │   ├── docker-compose.service.yml   # 应用服务（生产）
│   │   ├── docker-compose.watch.yml     # 应用服务（开发热重载）
│   │   ├── .env.example
│   │   ├── .env                (需要手动创建)
│   │   └── Dockerfile
│   └── bin/
│       ├── run                 # Linux/Mac 启动脚本
│       └── run.py              # Python 启动脚本
│
├── frontend/
│   ├── src/
│   │   ├── components/         # Vue 组件
│   │   ├── views/              # 页面
│   │   ├── services/           # API 服务（调用后端）
│   │   ├── stores/             # Pinia 状态管理
│   │   └── types/              # TypeScript 类型
│   ├── vite.config.ts          # Vite 配置（包含 API 代理）
│   ├── package.json
│   └── tsconfig.json
```

---

## 总结

快速启动（3 步）：

1️⃣ **启动后端**（约 1 分钟）
```bash
cd backend && ./bin/run up --build -d
```

2️⃣ **启动前端**
```bash
cd frontend && npm install && npm run dev
```

3️⃣ **打开浏览器**
访问 `http://localhost:5173`

✅ 完成！现在可以开始开发和测试了。
