# 前后端接口字段差异分析及统一建议

> ✅ **更新说明**：前端类型定义已完成修改，与后端保持一致。后端待补充字段见 `后端待补充接口字段.md`

## 一、概述

本文档分析了 HEUMathOverflow 项目中前后端接口的字段差异，并给出统一建议。

### 已解决的问题 ✅

1. ~~**命名风格不一致**~~：前端已统一采用蛇形命名（snake_case）
2. ~~**字段语义差异**~~：status 字段已统一使用数字（1/2/3）
3. ~~**ID 类型不一致**~~：前端已统一使用 `string` 类型
4. ~~**分页机制差异**~~：前端已适配 offset/limit 模式

### 待后端补充的问题 ⚠️

1. 回复缺少 `likes` 字段
2. 分页接口缺少 `total` 总数
3. 交互状态需单独查询（建议聚合返回）
4. 部分接口缺失（标签、搜索、认证回复等）

---

## 二、前端类型修改清单

### 2.1 types/common.ts - 公共常量和类型

新增以下内容：

```typescript
// 角色常量
export type UserRole = 1 | 2 | 3 | 4; // 1:学生 2:助教 3:教师 4:管理员
export const RoleMap = { 1: "学生", 2: "助教", 3: "教师", 4: "管理员" };

// 帖子状态常量
export type PostStatus = 1 | 2 | 3; // 1:未解决 2:已解决 3:已认证
export const PostStatusMap = { 1: "未解决", 2: "已解决", 3: "已认证" };

// 回复状态常量
export type ReplyStatus = 1 | 2 | 3; // 1:未精选 2:作者精选 3:教师精选
export const ReplyStatusMap = { 1: "未精选", 2: "作者精选", 3: "教师精选" };

// 排序类型
export type OrderBy = 0 | 1 | 2; // 0:推荐 1:最热 2:最新
```

### 2.2 types/user.ts - 用户类型

```typescript
export interface UserInfo {
  id: string; // ✅ 统一为 string（原 number）
  username: string;
  role: UserRole; // ✅ 统一为数字（原混合类型）
  avatar_url: string; // ✅ 保持蛇形命名
}
// ❌ 已移除 password_hash（安全问题）
// ❌ 已移除 email（敏感信息）
// ❌ 已移除 created_at/last_login（后端不返回）
```

### 2.3 types/forum.ts - 论坛类型

**Author 类型：**

```typescript
export interface Author {
  user_id: string; // ✅ 原 id
  username: string;
  avatar_url: string; // ✅ 原 avatar
  role: UserRole; // ✅ 统一为数字
}
```

**Post 类型：**

```typescript
export interface PostData {
  post_id: string; // ✅ 原 id
  title: string;
  content: string;
  tags: string[];
  image_urls: string[]; // ✅ 新增
  status: PostStatus; // ✅ 原 'pending'|'approved'|'rejected'
  views: number; // ✅ 原 viewCount
  likes: number; // ✅ 原 likeCount
  stars: number; // ✅ 新增
  replies: number; // ✅ 原 replyCount
  last_reply_at: string | null; // ✅ 新增
  created_at: string; // ✅ 原 createdAt
  updated_at: string; // ✅ 原 updatedAt
}
// ❌ 已移除 category（后端未实现）
// ❌ 已移除 isAnonymous（后端未实现）
// ❌ 已移除 hasCertifiedAnswer（改用 status === 3 判断）
```

**Reply 类型：**

```typescript
export interface ReplyData {
  reply_id: string; // ✅ 原 id
  post_id: string; // ✅ 原 postId
  parent_reply_id: string | null; // ✅ 原 parentId
  content: string;
  status: ReplyStatus; // ✅ 原 'pending'|'approved'|'rejected'
  image_urls: string[]; // ✅ 新增
  voice_url: string; // ✅ 新增
  voice_text: string; // ✅ 新增
  ai_answered: boolean; // ✅ 新增
  certified_by: string | null; // ✅ 新增
  created_at: string; // ✅ 原 createdAt
  likes?: number; // ⚠️ 后端需补充返回
}
// ❌ 已移除 isAnonymous（后端未实现）
// ❌ 已移除 isCertified（改用 status === 2 || status === 3 判断）
```

**分页参数：**

```typescript
export interface GetPostsParams {
  offset?: number; // ✅ 原 page
  limit?: number;
  order?: OrderBy; // ✅ 原 sortBy + sortOrder
}
```

---

## 三、字段映射对照表

### 3.1 已统一字段 ✅

| 类别 | 旧前端字段     | 新前端字段        | 后端字段          |
| ---- | -------------- | ----------------- | ----------------- |
| 帖子 | `id`           | `post_id`         | `post_id`         |
| 帖子 | `createdAt`    | `created_at`      | `created_at`      |
| 帖子 | `updatedAt`    | `updated_at`      | `updated_at`      |
| 帖子 | `likeCount`    | `likes`           | `likes`           |
| 帖子 | `viewCount`    | `views`           | `views`           |
| 帖子 | `replyCount`   | `replies`         | `replies`         |
| 帖子 | `isBookmarked` | `is_starred`      | `starred`         |
| 回复 | `id`           | `reply_id`        | `reply_id`        |
| 回复 | `postId`       | `post_id`         | `post_id`         |
| 回复 | `parentId`     | `parent_reply_id` | `parent_reply_id` |
| 回复 | `createdAt`    | `created_at`      | `created_at`      |
| 作者 | `id`           | `user_id`         | `user_id`         |
| 作者 | `avatar`       | `avatar_url`      | `avatar_url`      |
| 用户 | `id: number`   | `id: string`      | `id: string`      |

### 3.2 状态字段统一 ✅

| 类别     | 旧前端定义                          | 新前端定义 | 后端定义                         |
| -------- | ----------------------------------- | ---------- | -------------------------------- |
| 帖子状态 | `'pending'\|'approved'\|'rejected'` | `1\|2\|3`  | `1:未解决 2:已解决 3:已认证`     |
| 回复状态 | `'pending'\|'approved'\|'rejected'` | `1\|2\|3`  | `1:未精选 2:作者精选 3:教师精选` |

### 3.3 已移除字段 ❌

| 类别 | 字段                 | 原因                     |
| ---- | -------------------- | ------------------------ |
| 用户 | `password_hash`      | 安全问题，不应暴露       |
| 用户 | `email`              | 敏感信息，登录后不返回   |
| 帖子 | `category`           | 后端未实现               |
| 帖子 | `isAnonymous`        | 后端未实现               |
| 帖子 | `hasCertifiedAnswer` | 改用 `status === 3` 判断 |
| 回复 | `isAnonymous`        | 后端未实现               |
| 回复 | `isCertified`        | 改用 `status >= 2` 判断  |

### 3.4 新增字段 ✨

| 类别 | 字段            | 说明         |
| ---- | --------------- | ------------ |
| 帖子 | `stars`         | 收藏数       |
| 帖子 | `image_urls`    | 图片列表     |
| 帖子 | `last_reply_at` | 最后回复时间 |
| 回复 | `image_urls`    | 图片列表     |
| 回复 | `voice_url`     | 语音 URL     |
| 回复 | `voice_text`    | 语音转文字   |
| 回复 | `ai_answered`   | AI 回复标记  |
| 回复 | `certified_by`  | 精选认证人   |

---

## 四、后端待补充清单

详见 `后端待补充接口字段.md`，摘要如下：

### P0 - 必须补充

| 项目              | 说明                                   |
| ----------------- | -------------------------------------- |
| 回复 `likes` 字段 | `GET /posts/{id}/replies` 响应中需返回 |
| 分页 `total`      | 所有分页接口需返回总数                 |

### P1 - 高优先级

| 项目               | 说明                                        |
| ------------------ | ------------------------------------------- |
| 交互状态聚合       | 帖子/回复列表中返回 `is_liked`/`is_starred` |
| 更新用户信息返回值 | `PATCH /user/profile` 返回更新后的用户信息  |

### P2 - 中优先级

| 项目         | 说明                         |
| ------------ | ---------------------------- |
| 标签列表接口 | `GET /tags`                  |
| 按标签筛选   | `GET /posts?tags=xxx`        |
| 搜索接口     | `GET /posts?search=xxx`      |
| 删除回复接口 | `DELETE /replies/{id}`       |
| 认证回复接口 | `POST /replies/{id}/certify` |

---

## 五、使用指南

### 5.1 状态判断

```typescript
import { PostStatusMap, ReplyStatusMap, isReplyCertified } from "@/types";

// 获取帖子状态文本
const statusText = PostStatusMap[post.status]; // '未解决' | '已解决' | '已认证'

// 判断帖子是否已认证
const isCertified = post.status === 3;

// 判断回复是否精选
const isCertified = isReplyCertified(reply.status); // status === 2 || status === 3
```

### 5.2 角色判断

```typescript
import { RoleMap, getRoleText } from "@/types";

// 获取角色文本
const roleText = getRoleText(user.role); // '学生' | '助教' | '教师' | '管理员'

// 判断是否为教师或以上
const isTeacherOrAbove = user.role >= 3;
```

### 5.3 分页转换

```typescript
// offset 模式（后端）
const offset = (page - 1) * limit;

// page 模式（前端显示）
const page = Math.floor(offset / limit) + 1;
```

---

## 六、迁移影响范围

以下组件/视图可能需要更新字段名：

- `HomeView.vue` - 帖子列表展示
- `PostDetailView.vue` - 帖子详情和回复
- `CreatePostView.vue` - 创建帖子
- `ProfileView.vue` - 用户信息展示
- `UserMenu.vue` - 用户菜单

**建议**：使用 IDE 的查找替换功能，按照上述映射表进行批量替换。
