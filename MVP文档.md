# 《公共数学智慧答疑与社区认证系统》MVP 阶段：基础论坛功能文档

## 一、 项目简介与 MVP 目标

本阶段（MVP，最小可行性产品）旨在快速搭建一个**基础的在线答疑论坛**。
核心目标是建立一个供学生提问和教师/同学解答的平台，验证基本的用户交互流程，并为后续接入 AI 助教和数据收集奠定基础。

**本阶段暂不包含：** AI 自动回答、知识库训练、AI 自动内容审核、复杂的数据导出与训练流程。

## 二、 MVP 功能需求

### 2.1 用户角色

1.  **学生**：普通用户，主要进行提问和参与讨论。
2.  **教师/管理员**：管理用户，负责维护社区秩序，解答难题或确认优质答案。

### 2.2 核心功能模块

#### 模块一：用户认证系统

- **注册/登录**：支持账号密码注册与登录。
- **个人信息**：简单的个人资料展示（昵称、头像）。
- **权限控制**：基于角色的访问控制（RBAC），区分普通用户与管理员权限。

#### 模块二：论坛问答（核心）

- **问题列表**：
  - 展示所有问题帖子。
  - 支持按时间排序（最新发布的在前）。
  - 简单的状态标记（如：未解决/已解决）。
- **发布问题（发帖）**：
  - 输入标题和内容。
  - **支持 Markdown 与 LaTeX 公式**（数学论坛必备）。
  - 支持选择简单的标签（如：高数、线代）。
- **帖子详情与回复**：
  - 查看完整问题内容。
  - **发布回复**：支持富文本/Markdown+LaTeX。
  - 查看回复列表。

#### 模块三：基础管理与交互

- **优质答案标记**（教师权限）：
  - 教师可以将某个回复标记为“推荐答案”或“正确答案”，该回复在 UI 上会有特殊高亮显示。
  - (这是“认证”功能的 MVP 版本，用于手动筛选高质量内容)。
- **内容管理**（教师权限）：
  - 删除违规的帖子或回复。
  - 置顶重要通知或常见问题。

## 三、 技术栈（MVP 建议）

- **前端**：Vue.js + TypeScript + UI 组件库 (如 Element Plus 或 Ant Design Vue)。
  - 重点依赖：Markdown 编辑器（如 vditor 或 tiptap）、LaTeX 渲染库（KaTeX 或 MathJax）。
- **后端**：Go (Gin/Zero) 或 Python (FastAPI)。
  - 提供 RESTful API。
- **数据库**：MySQL 或 PostgreSQL。
  - 存储用户、帖子、回复数据。

## 四、 后续迭代规划（非 MVP 内容）

MVP 上线稳定后，将按以下顺序迭代：

1.  **接入 AI 助教**：监听新帖子，调用大模型 API 自动生成回复。
2.  **内容安全审核**：集成云服务商 API 进行自动过滤。
3.  **数据沉淀系统**：开发专门的后台，将“教师标记的优质答案”导出为训练数据集。
