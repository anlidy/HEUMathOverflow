# 后端待补充接口字段清单

本文档列出了前端需要但后端当前未提供的字段和接口，按优先级排序。

---

## 一、P0 - 必须补充（影响核心功能）

### 1.1 回复的点赞数字段

**问题描述：** 后端 `reply_data` 未返回 `likes` 字段，导致前端无法显示回复的点赞数。

**当前响应结构：**

```json
{
  "reply_data": {
    "reply_id": "...",
    "content": "..."
    // 缺少 likes 字段
  }
}
```

**建议补充：**

```json
{
  "reply_data": {
    "reply_id": "...",
    "content": "...",
    "likes": 10 // 新增
  }
}
```

**涉及接口：**

- `GET /api/v1/forum/posts/{postID}/replies`

---

### 1.2 分页信息返回

**问题描述：** 后端分页接口未返回总数（`total`），前端无法正确显示分页组件。

**当前响应结构：**

```json
{
    "code": 200,
    "data": [...],
    "message": "success"
}
```

**建议补充：**

```json
{
    "code": 200,
    "data": [...],
    "pagination": {
        "offset": 0,
        "limit": 10,
        "total": 156,
        "has_more": true
    },
    "message": "success"
}
```

**涉及接口：**

- `GET /api/v1/forum/posts`
- `GET /api/v1/forum/posts/{postID}/replies`
- `GET /api/v1/forum/posts/starred`

---

## 二、P1 - 高优先级（提升用户体验）

### 2.1 帖子/回复交互状态聚合返回

**问题描述：** 当前获取帖子列表/详情时，需要额外请求来判断用户是否已点赞/收藏，增加了请求次数。

**当前设计：**

1. `GET /posts` 获取帖子列表
2. 对每个帖子调用 `GET /posts/like/{id}` 查询点赞状态
3. 对每个帖子调用 `GET /posts/star/{id}` 查询收藏状态

**建议优化：**

在获取帖子列表/详情时，直接返回当前用户的交互状态：

```json
{
    "post_data": {
        "post_id": "...",
        // ... 其他字段
    },
    "user_info": { ... },
    "interaction": {  // 新增：当前用户的交互状态
        "is_liked": true,
        "is_starred": false
    }
}
```

或在 `post_data` 中直接包含：

```json
{
  "post_data": {
    "post_id": "...",
    // ... 其他字段
    "is_liked": true, // 新增
    "is_starred": false // 新增
  }
}
```

**涉及接口：**

- `GET /api/v1/forum/posts`
- `GET /api/v1/forum/posts/{post_id}`
- `GET /api/v1/forum/posts/{postID}/replies`（回复的 `is_liked`）

---

### 2.2 修改用户信息后返回更新后的用户信息

**问题描述：** `PATCH /api/v1/user/profile` 只返回 `code` 和 `message`，前端需要再次请求获取最新用户信息。

**当前响应：**

```json
{
  "code": 200,
  "message": "success"
}
```

**建议补充：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "user_info": {
      "id": "...",
      "username": "new_name",
      "role": 1,
      "avatar_url": "..."
    }
  }
}
```

**涉及接口：**

- `PATCH /api/v1/user/profile`

---

## 三、P2 - 中优先级（功能完善）

### 3.1 获取标签列表接口

**问题描述：** 前端需要展示可用标签列表供用户选择，但后端未提供此接口。

**建议新增接口：**

```
GET /api/v1/forum/tags
```

**响应结构：**

```json
{
  "code": 200,
  "data": {
    "tags": [
      { "name": "数学分析", "count": 42 },
      { "name": "线性代数", "count": 38 },
      { "name": "概率论", "count": 25 }
    ]
  },
  "message": "success"
}
```

---

### 3.2 按标签筛选帖子

**问题描述：** 前端需要按标签筛选帖子，但后端 `GET /posts` 接口不支持 `tags` 参数。

**当前接口：**

```
GET /api/v1/forum/posts?offset=0&limit=10&order=0
```

**建议支持：**

```
GET /api/v1/forum/posts?offset=0&limit=10&order=0&tags=数学分析,线性代数
```

---

### 3.3 搜索帖子接口

**问题描述：** 前端需要提供搜索功能，但后端未支持。

**建议支持：**

```
GET /api/v1/forum/posts?offset=0&limit=10&search=积分求解
```

或新增独立搜索接口：

```
GET /api/v1/forum/posts/search?q=积分求解&offset=0&limit=10
```

---

### 3.4 删除回复接口

**问题描述：** 后端接口文档未明确提供删除回复的接口。

**建议确认/补充：**

```
DELETE /api/v1/forum/replies/{reply_id}
```

---

### 3.5 认证/精选回复接口

**问题描述：** 前端需要支持帖子作者或教师精选回复作为最佳答案，但后端未提供此接口。

**建议新增接口：**

```
POST /api/v1/forum/replies/{reply_id}/certify
```

**请求参数：**

```json
{
  "post_id": "帖子ID" // 用于权限校验
}
```

**响应：**

```json
{
  "code": 200,
  "message": "success"
}
```

---

## 四、P3 - 低优先级（后续迭代）

### 4.1 匿名发帖/回复功能

前端类型中保留了 `is_anonymous` 字段，但后端未实现。如需此功能，需要在创建帖子/回复时支持 `is_anonymous` 参数，并在返回数据中隐藏真实用户信息。

### 4.2 分类功能

前端设计中有 `category` 分类字段，但后端未实现。如需此功能，可考虑：

- 在帖子表中增加 `category` 字段
- 提供分类列表接口
- 支持按分类筛选帖子

### 4.3 回复的更新时间

后端 `reply_data` 未返回 `updated_at` 字段。如果回复支持编辑功能，建议补充此字段。

---

## 五、字段命名对照表

为方便前后端协作，以下是字段命名对照：

| 前端字段（新）    | 后端字段          | 说明                            |
| ----------------- | ----------------- | ------------------------------- |
| `post_id`         | `post_id`         | ✅ 已统一                       |
| `reply_id`        | `reply_id`        | ✅ 已统一                       |
| `user_id`         | `user_id`         | ✅ 已统一                       |
| `created_at`      | `created_at`      | ✅ 已统一                       |
| `updated_at`      | `updated_at`      | ✅ 已统一                       |
| `avatar_url`      | `avatar_url`      | ✅ 已统一                       |
| `parent_reply_id` | `parent_reply_id` | ✅ 已统一                       |
| `image_urls`      | `image_urls`      | ✅ 已统一                       |
| `voice_url`       | `voice_url`       | ✅ 已统一                       |
| `voice_text`      | `voice_text`      | ✅ 已统一                       |
| `ai_answered`     | `ai_answered`     | ✅ 已统一                       |
| `certified_by`    | `certified_by`    | ✅ 已统一                       |
| `last_reply_at`   | `last_reply_at`   | ✅ 已统一                       |
| `likes`           | `likes`           | ✅ 已统一（帖子有，回复需补充） |
| `views`           | `views`           | ✅ 已统一                       |
| `stars`           | `stars`           | ✅ 已统一                       |
| `replies`         | `replies`         | ✅ 已统一                       |
| `status`          | `status`          | ✅ 已统一（使用数字 1/2/3）     |
| `is_liked`        | ⚠️ 待补充         | 建议聚合返回                    |
| `is_starred`      | ⚠️ 待补充         | 建议聚合返回                    |
| `total`           | ⚠️ 待补充         | 分页总数                        |

---

## 六、实施建议

1. **第一阶段**：补充 `likes` 字段和分页 `total`（P0）
2. **第二阶段**：实现交互状态聚合返回（P1）
3. **第三阶段**：实现标签、搜索、认证等功能（P2）
4. **后续迭代**：根据产品需求实现匿名、分类等功能（P3）
