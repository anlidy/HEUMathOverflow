root = "../.."
tmp_dir = "tmp_forum"

[build]
  cmd = "go build ."
  bin = "./forum-service"
  full_bin = "./forum-service"
  include_ext = ["go", "yaml", "mod", "sum"]
  exclude_dir = ["tmp_", "vendor", "bin", "deploy", "proto"]
  include_dir = ["services/forum", "common", "api/user"]
  delay = 1000

[log]
  time = true

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"
