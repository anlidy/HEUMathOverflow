apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f ../docker-compose.service.yml convert
    kompose.version: 1.37.0 (fb0539e64)
  labels:
    io.kompose.service: audit-service
  name: audit-service
spec:
  ports:
    - name: "8083"
      port: 8083
      targetPort: 8083
  selector:
    io.kompose.service: audit-service
