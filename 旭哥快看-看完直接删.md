我先简短说明 Docker Compose 的启动顺序与常见误解，然后给出可操作的三种解决方案（推荐在服务里做重试/等待 + 在 compose 中添加 healthcheck），并提供检查/验证命令与示例配置片段，帮助你解决 “connection refused” 问题。

## 启动顺序要点
- Docker Compose 会按依赖关系（`depends_on`）的顺序创建并启动容器——即被依赖的服务会先被创建并启动，但这仅保证容器进程已“启动”（running），**并不保证服务已经可用（ready）**。  
- 因此 `depends_on: - postgres` 只会在创建/启动 postgres 容器之后再启动 `user-service`，但不会等待 Postgres 完全接受连接（例如完成初始化、接受 TCP 连接、完成用户/数据库创建等）。这正是你看到 Go 程序连接被拒绝（connection refused）的常见原因：Postgres 进程还没完全准备好就被访问了。

## 怎么解决（三种常用方法）
下面按推荐顺序给出方案，通常同时采用第 1（在服务里重试）+ 第 2（在 compose 加 healthcheck）即可覆盖大多数场景。

1) 在应用（Go 服务）中实现连接重试（强烈推荐）
- 在启动阶段对 DB 连接做带退避的重试（比如 5-30 次，指数退避），直到连接成功或超时再继续。这样无论容器如何启动顺序，服务都会稳健等待 DB 就绪。
- 简单示例（伪代码）：
  ```go
  for i := 0; i < 12; i++ {
      db, err := sql.Open("postgres", dsn)
      if err == nil {
          err = db.Ping()
      }
      if err == nil {
          // success
          break
      }
      time.Sleep(time.Second * time.Duration(math.Pow(2, float64(i))))
  }
  ```
- 优点：最可靠、与 Compose 无关、可应对网络波动与 DB 重启。

2) 在 Compose 中为 Postgres 添加 healthcheck（配合启动等待）
- 在 `docker-compose.yml` 的 `postgres` 服务加入 healthcheck，用 `pg_isready` 检测数据库是否能接受连接。然后可以选择性地在 `user-service` 的 entrypoint 里等待 `postgres` 的健康状态或使用 `depends_on` 的健康条件（见说明）。
- healthcheck 示例：
  ```yaml
  services:
    postgres:
      image: postgres:17
      environment:
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        POSTGRES_DB: ${POSTGRES_DB:-user_db}
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB} || exit 1"]
        interval: 5s
        timeout: 5s
        retries: 5
        start_period: 5s
  ```
- 注意：Compose 文件格式的 `depends_on` 支持基于 health 的条件只有在旧的 `version: '2.1'` 格式里有 `condition: service_healthy`。在 v3+（或现代 Docker Compose 用法）里 `depends_on` 不再支持 `condition`；因此即使加了 healthcheck，compose 也不会自动阻塞等待健康 — 仍需在 consumer（user-service）那边等待或做重试。

3) 在 `user-service` 容器里用“等待脚本”在启动时等待 Postgres 就绪
- 在 `user-service` 的 Dockerfile 或 entrypoint 加入例如 `wait-for` 脚本或简单循环调用 `pg_isready` / `nc` / `bash` 检查：
  ```sh
  # entrypoint.sh
  until pg_isready -h postgres -p 5432 -U "$POSTGRES_USER" -d "$POSTGRES_DB"; do
    echo "Waiting for postgres..."
    sleep 1
  done
  exec /path/to/your/app   # 启动 Go 程序
  ```
- 这种方式配合 healthcheck 非常稳健。注意容器内需要安装 `postgres-client` 或包含 `pg_isready`。

## 如何检查当前状态（调试步骤）
在 docker 目录下运行（示例）：
```bash
# 启动服务（如果还没启动）
docker compose -f backend/docker/docker-compose.yml up -d --build

# 查看容器状态
docker compose -f backend/docker/docker-compose.yml ps

# 查看 postgres 日志（查看是否启动出错）
docker compose -f backend/docker/docker-compose.yml logs -f postgres

# 查看某个容器的 Health 状态（如果有 healthcheck）
docker inspect --format='{{json .State.Health}}' <container_name_or_id> | jq .
```

如果看到 postgres 容器不断重启或初始化失败，重点看 postgres 日志里有没有权限/初始化文件/磁盘挂载问题。

## 推荐的最小改动（可直接应用）
- 在 docker-compose.yml 给 `postgres` 加上上面的 `healthcheck`。  
- 在 `user-service` 的容器启动脚本（或 Dockerfile 的 ENTRYPOINT）里加 `pg_isready` 循环，或在 Go 程序中实现重试逻辑。

示例把两者结合：Compose 加 healthcheck，然后在 `user-service` entrypoint 等待 pg_isready（更健壮）。

## 额外提示
- 检查 `POSTGRES_*` 环境变量是否被正确设置（你的 compose 使用 ${POSTGRES_USER} 等）。如果这些变量为空，Postgres 可能会以默认账号启动或失败。建议在 docker 目录下放一个 `.env`（或 `.env.example`）来确保这些变量被填上。
- 如果 Postgres 数据卷目录（`./data/Postgre`）因权限或残留数据（旧版本）导致初始化失败，容器会启动但 DB 进程可能退出。若遇到这类问题，查看 `postgres` 日志是关键。

---

需要的话我可以：
- 帮你在 docker-compose.yml 中添加 `healthcheck` 的片段（直接替换/编辑文件）。  
- 或者帮你在 `user-service` 镜像中添加一个 `entrypoint.sh` 等待脚本并修改 Dockerfile（我可以直接修改 repo 并运行一次验证命令）。  

你想先让我帮你把 `healthcheck` 加到 `docker-compose.yml`，还是直接在 `user-service` 中添加等待脚本/在 Go 启动里添加重试？